"""看板层（模块三）：基建 + 四个页面共用组件。

- `theme`：统一配色与 Plotly 中文图表模板（色板经色盲安全校验，改动须重跑校验）
- `data`：产物读取层，全部带缓存；缺产物即报错、不静默降级
- `filters`：侧边栏全局筛选器（日期范围 / 品类 / 配送区域 / 成本口径）
- `kpis`：KPI 窗口聚合与环比（**纯函数，不依赖 Streamlit**，可单测）
- `components`：KPI 卡片、趋势图、图表块、异常预警清单

运行：在项目根执行 `streamlit run app.py`
"""

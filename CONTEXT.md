# 区域仓配中心运营数据分析与配送路径优化

一个面向物流 / 供应链运营岗位的求职作品集工程。以「真实电商订单 + 真实地理路网 + 学术标准算例 + 仓内过程仿真」四类数据为底座，模拟某区域配送中心连续 90 天的仓储与配送运营，走通数据生成 → 仓储分析 → 仓内仿真 → 运输优化 → 看板 → PDCA 报告的全链路。最高原则是**数据四层分类**：每一份数据、报告中每一个数字都必须明确属于哪一类，禁止把仿真 / 假设表述为真实观测。

## Language

### 数据性质（贯穿全项目的最高分类原则）

**真实观测**:
Olist 真实电商订单、高德返回的真实 POI 坐标与真实路网里程 / 时间。
_Avoid_: 真数据、实际数据

**学术基准**:
Solomon VRPTW 标准算例及其公开最优解。
_Avoid_: 测试数据、标准数据

**过程仿真**:
SimPy 仓内作业仿真、仓内运营仿真表（入库 / 出库 / 盘点）。
_Avoid_: 模拟数据、假数据

**情景假设**:
配送订单需求、车辆参数、在途异常事件等以真实分布为参数构造的数据。参数必须来自真实数据统计，或在台账显式登记为假设并做敏感性分析。
_Avoid_: 构造数据、生成数据

### 准时率（同名不同义，必须分开）

**真实准时交付率**:
Olist 真实订单中，实际送达 ≤ 预计送达的订单占比。数据源为 Olist，反映真实电商履约水平。
_Avoid_: 准时交付率（单独使用，会歧义）

**时间窗达成率**:
成都配送订单中，实际送达落在要求时间窗（默认 08:00–18:00 内 2 小时窗）内的订单占比。数据源为配送情景 + VRPTW 优化结果，是项目主线成果、驾驶舱 KPI 卡片默认显示项。在「单点一访」语义下按需求聚合的到访时刻逐单判定。
_Avoid_: 准时交付率、准时率（单独使用，会歧义）

**配送订单（数据性质）**:
成都配送订单表整体为**情景假设**数据：POI 坐标为真实观测，订单数按泊松过程生成，重量 / 体积的分布**形状**借用自 Olist 真实观测（Olist 本身非冷链，仅作为「典型订单多重多大」的经验分布来源，不声称是 Olist 商品）。

### 仓储分析

**ABC 分类**:
按**仿真仓出库行数**累计占比将 SKU 划分为 A/B/C（默认 70%:20%:10%，阈值可调）。数据源是 3.1 仿真仓的 500 个 SKU，**不是** Olist。它直接驱动库位重排与仓内仿真实验一，因此必须与库位体系同源。
_Avoid_: 帕累托分类、Olist ABC（本项目 Olist 侧不算 ABC）

**库位重排**:
将 A 类高频 SKU 从远拣货台库位重排至最近库位，以 Σ(出库频次 × 库位距离) 为拣货行走成本衡量重排前后收益。与仓内仿真实验一互相印证。

**拣货效率**:
拣货总行数 / Σ(拣货完成−拣货开始，按小时计)，单位「行 / 人时」。

### 运输优化

**冷链城配**:
成都场景的设定：区域仓 DC 为冷链城配中心（服务超市 / 便利店 / 驿站最后一公里的生鲜医药配送），车厢温度 [2,8℃] 全程监控。该设定使 POI 类型、温控达标率 KPI 与温控波动埋点三者自洽。

**需求聚合**:
将同一 POI 当日全部配送订单合并为一个需求点——重量 / 体积求和、时间窗取各订单窗的外包络 [最早起, 最晚止]，VRPTW 节点 = POI、单点单次到访；时间窗达成率按到访时刻**逐单**判定。
_Avoid_: 订单合并、拼单

**代表日**:
90 天中订单量最大的典型工作日。OR-Tools VRPTW 的完整「基线 vs 优化」精算只在此日进行（出 GeoJSON、单趟明细、看板地图切换）；其余 89 天用基线贪心覆盖全量里程 / 成本 / 准时率，优化收益按代表日的单均节省率外推到月 / 年。

**基线策略**:
最近邻贪心，模拟「人工就近派车」，使用真实路网距离 / 时间矩阵（禁止直线距离）。

**自营 / 外包对标**:
两套自营模式（柴油自购、纯电租赁）做 TCO 对比与盈亏平衡里程；货拉拉外包市场价仅作决策对照，不进自营车队。

## 数据来源与参数标定台账

见 `data_sources_ledger.md`（持续维护）。凡假设参数必须附敏感性分析结论；报告与简历可用的每个数字都必须能在台账追溯。

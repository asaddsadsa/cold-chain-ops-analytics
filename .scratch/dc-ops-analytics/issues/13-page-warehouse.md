# 13: 看板模块三：仓储分析页

**What to build:** 看板用户在仓储分析页看到完整的仓内 + Olist 仓储诊断可视化：ABC 帕累托图；分时段拣货效率柱状图（必须能看出 14–16 点低谷）；库位出库频次热力图（800 库位网格）；盘点差异 Top10 品类表（P03 居前）；Olist 真实履约时长分布与延迟区域下钻图；仿真对照实验结果图，配拣货员人数滑块实时切换 4/5/6 人结果（读数据层 F 预计算缓存）。继承驾驶舱页的全局筛选/缓存/中文图表基建。

**Blocked by:** 12（看板基建）、07（仿真实验预计算缓存）、09（Olist 履约 KPI）

**Status:** done（已实现并在真实运行时逐页验收，见文末实现记录）

- [x] ABC 帕累托图（数据源=仿真仓 ABC，ADR-0002）
- [x] 分时段拣货效率柱状图，可见 14–16 点低谷埋点
- [x] 库位出库频次热力图（800 库位网格）
- [x] 盘点差异 Top10 品类表，P03 居前
- [x] Olist 真实履约时长分布 + 延迟区域下钻图
- [x] 仿真对照实验结果图 + 拣货员人数滑块切 4/5/6 档（读 07 预计算缓存，瞬时响应）
- [x] 全图表中文标题/坐标轴/单位、悬停明细、统一配色、读数缓存
- [x] 全局筛选生效，整页无报错

## Comments

> *This was generated by AI during implementation（2026-09-14）.*

### 实现记录

**产出**：`pages/1_仓储分析.py`（22 KB）+ 配套的 `src/dashboard/charts.py`（图表构造器，与
`components.py` 的页面骨架分工）与 `src/dashboard/data.py` 的 8 个新读数入口。

**8 个区块**：逐日仓内指标（日期筛选生效）、ABC 帕累托、分时段拣货效率（14/15 点高亮）、
800 库位频次热力图（20×40 网格、无出库库位补 0 显示）、盘点差异 Top10（P03 高亮）、
Olist 履约时长分布 + 州下钻、SimPy 实验一对照（含 95% CI）与人力滑块切 4/5/6 档、数据来源表。

**两处按规范做的取舍**：
- **帕累托图不用双 Y 轴**。常见画法是「数量画柱 + 累计占比画线」各配一根轴，那是双 Y 轴、两个
  刻度的相对位置任意。这里把柱也换成**占比**，两条序列同量纲共用一根 0–100% 的轴，帕累托的
  读法完整保留。
- **每张图都配表格视图**（11 张图 ↔ 11 处 `table=`）。浅色配色里青/黄/品红三槽对底色对比度
  低于 3:1，规范要求以「可见标签或表格视图」缓解——是必需项。

**验证**：语法 + 冒烟（`SMOKE OK` 无 Traceback）；真实运行时 `AppTest` 跑通，**0 异常**、
33 张图、11 张数据表；浏览器实测页面渲染正常。

# 15: 看板模块三：改善建议页

**What to build:** 看板用户能看到一页「问题诊断→改善动作→预期收益」表格——条目由分析结果自动汇总、每个数字引用结果产物文件（不硬编码）；并用 what-if 控件交互验证改善逻辑：车辆数量 5–20 滑块（读 ADR-0010 的 16 档预计算缓存，瞬时切档）、ABC 分类阈值滑块（实时重算分类）、拣货员人数（联动仿真实验缓存 4/5/6 档）、动力模式切换（柴油自购/纯电租赁）与司机工资/能源价格/租金三档敏感性选择（联动 TCO 曲线）。控件变化后相关 KPI 实时重算且无报错。

**Blocked by:** 13（仓储分析页）、14（运输分析页）

**Status:** done（已实现并在真实运行时逐页验收，见文末实现记录）

- [x] 「问题诊断→改善动作→预期收益」表自动汇总自分析产物，数字可追溯到结果文件
- [x] 车辆数 5–20 滑块读 16 档预计算缓存，瞬时刷新，超出网格的参数组合明确提示「需重跑预计算脚本」
- [x] ABC 阈值滑块实时重算分类并联动帕累托展示
- [x] 拣货员人数控件联动仿真实验缓存档位
- [x] 动力模式切换 + 工资/能源/租金三档敏感性联动 TCO 曲线
- [x] 所有控件变化后 KPI 实时重算、整页无报错
- [x] 全局筛选生效，图表中文/配色/缓存与基建一致

## Comments

> *This was generated by AI during implementation（2026-09-14）.*

### 实现记录

**产出**：`pages/3_改善建议.py`（40 KB）。它是 PDCA 报告的可交互版本，逐条对齐 P→D→C。

**四组 what-if 控件，全部读预计算缓存、瞬时响应**：
1. **车辆数 5–20**（16 档预计算）——走 `transport_decisions.whatif_lookup`，含超网格
   「需重跑预计算脚本」提示。**不可行档位是结论本身**：拖到 5–8 档时页面显示
   `infeasible_reason`（当日货量 10.8 t、1.4 t 额定至少需 8 台车）**而不是画成 0**。
2. **ABC 双阈值滑块**——调 `warehouse_kpi.abc_classify` **实时重算**（不在页面里重写分类逻辑），
   缓存键含两个阈值，否则拖动不生效。实测 A 阈值 0.70→0.71 时 A 类由 141 个 SKU 变为 148 个。
3. **拣货员 4/5/6 档**——读 `sim/whatif_staffing.json`，配拐点（5 人）与边际收益。
4. **动力模式 + 工资/能源/租金三档敏感性**——走 `config` 的成本函数；页面写明敏感性是
   **one-at-a-time**（变一个、其余留 mid），不把三个参数同时变的结果混着展示。

**「诊断→动作→收益」表** 7 行，每行带**证据文件路径**与**数据类别**（真实观测/学术基准/
过程仿真/情景假设）。其中**刻意保留一行「时间窗达成率未改善」**——基线与优化同为 79.55%，
如实写出比硬造一个「优化使准时率提升 x%」诚实得多。

**唯一有意偏离票据之处**：本页读数是全量口径的标量（车队/仓/代表日），**没有日期、品类、
片区维度**，故全局筛选对本页不生效。按「如实声明、不假装生效」的规范，页面顶端明确写出
「本页为全量口径，全局筛选不改变下列读数」，而不是把「全部品类」当成一个筛过的结果展示。

**实现期自查抓到一个真错**：`config.cost_fixed_per_day` 不接受 `energy_price` 参数，
首次冒烟即报错并修正——冒烟测试的价值正在于此。

**验证**：`AppTest` 真实运行时 0 异常；控件逐个驱动实测——车辆数 9 → 945.7 km / 满载 85.6% /
29.33 元（与预计算缓存逐档吻合）、车辆数 5 → 显示不可行原因、ABC 阈值与动力模式切换均即时重算。

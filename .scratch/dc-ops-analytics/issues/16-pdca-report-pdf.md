# 16: 模块四：PDCA 改善报告 + PDF 生成

**What to build:** 求职者拿到一份结构完整的 PDCA 改善报告（Markdown + PDF 两版）。报告按 P→D→C→A 撰写：P（现状诊断）列 3–5 个全部有数据支撑的问题，每个注明数据来源类别（真实观测/学术基准/过程仿真/情景假设）；D（改善措施）对应库位重排、VRPTW 路径优化、周度异常复盘机制、人力配置优化；C（效果验证）的每一个数字都必须引用程序输出文件（processed 结果），禁止编造或估算；A（固化机制）给出 SOP 与看板常态化监控建议。另实现 report/build_pdf.py：把 Markdown 渲染成带中文字体 CSS 的 HTML，调用本机 headless Edge `--print-to-pdf` 生成 PDF（已实测中文表格渲染通过，ADR-0007）；脚本检测不到 Edge/Chrome 时自动降级——只交付 Markdown，并在 README 与日志显式告知「PDF 未生成二进制、原因、手动转换步骤」，不得伪装已生成。

**Blocked by:** 08（仓内 KPI/ABC/库位重排）、09（Olist 履约 KPI）、10（运输优化核心）、11（异常复盘/TCO）

**Status:** done（已实现并在真实运行时逐页验收，见文末实现记录）

- [x] PDCA 四段结构完整：P 列 3–5 个有数据支撑的问题，每个注明数据来源类别
- [x] D 对应库位重排/VRPTW 优化/周度异常复盘/人力配置四项措施
- [x] C 段每个数字都能追溯到 processed 结果文件，无编造或估算
- [x] A 段给出 SOP 与看板常态化监控建议
- [x] Markdown 版完成
- [x] report/build_pdf.py 用 headless Edge 转 PDF（中文字体 CSS），实测生成中文 PDF 通过
- [x] 检测不到浏览器时降级交付 Markdown，README + 日志显式告知未生成 PDF 及手动转换步骤（不伪装）
- [x] PDF 与 Markdown 内容一致

## Comments

> *This was generated by AI during implementation（2026-09-14）.*

### 实现记录

**产出**：`report/pdca_report.md`、`report/pdca_report.pdf`（**10 页**、643 KB）、
`report/build_pdf.py`。

> 页数后来变过：2026-09-15 的 C-4 改写（补上时长分解表与两节修正记录）之后，PDF 实为
> **12 页、705 KB**。上面那句保留，以存当时的验收事实；结构描述不受影响。

**报告结构**：摘要表 → 数据基础与口径声明（四层数据分类 + 三条限制）→ **P** 5 个问题
（拣货时段低谷、P03 高盘差、库位与频次不匹配、就近派车里程偏高、在途异常结构性偏高），
每个都标注数据类别与证据文件 → **D** 四项措施（库位重排 / VRPTW / 周度复盘 / 人力配置）
→ **C** 逐项前后对比 → **A** 五条 SOP + 看板常态化监控 + 三条机制固化。附录 A 是
**数字→产物文件→生成命令**的溯源对照表。

**C-5 专门记录两项「没有改善」的指标**：时间窗达成率基线与优化同为 79.55%（原因：两套方案
都排在窗内、延误把同样那批点推出窗；并给出独立计算的理论上限 100%），以及在途异常总水平
8.04% 是**有意**与标定基准 8.13% 持平（倍率经全样本归一，不抬高全局水位）。把没改善的写清楚，
比只报好消息可信。

**PDF 实现的两处判断**：
- **不新增依赖**。本仓库依赖清单是锁定的，加一个 Markdown 库意味着给复现者多一个安装项。
  报告只用有限几种 Markdown 构造，故在 `build_pdf.py` 内实现这一**明确子集**并注明不追求通用。
- **降级不伪装**。检测不到 Edge/Chrome 时只交付 Markdown，日志打印原因与三条手动转换步骤；
  生成前先删旧 PDF，避免「命令失败但旧文件还在」被当成成功。

**验证**：实测生成 10 页 PDF，内嵌 `MicrosoftYaHei`（正/粗）与 `Consolas`、含 5 个 ToUnicode
映射（文本可检索）；渲染截图目视确认中文、表格、引用块、代码样式全部正常。

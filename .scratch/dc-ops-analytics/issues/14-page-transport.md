# 14: 看板模块三：运输分析页

**What to build:** 看板用户能在一页里看清运输优化全部成果——用 Folium 真实地图一键切换「优化前/优化后」配送路线并显示各点时间窗；里程/用车数/成本前后对比柱状图；满载率分布图；异常类型饼图 + 周趋势折线（可见周五/R07 埋点）；温控达标率仪表；柴油自购 vs 纯电租赁双模式 TCO 曲线（标注盈亏平衡里程）与「自营 vs 货拉拉外包」对照表。继承驾驶舱页的全局筛选/缓存/中文图表基建。

**Blocked by:** 12（看板基建）、11（异常复盘/TCO/外包对照结果）

**Status:** done（已实现并在真实运行时逐页验收，见文末实现记录）

- [x] Folium 真实地图绘配送路线，「优化前/后」一键切换，显示各点时间窗
- [x] 里程/用车数/成本前后对比柱状图（数字引用模块二产物）
- [x] 满载率分布图
- [x] 异常类型饼图 + 周趋势折线，可见周五/R07 高异常埋点
- [x] 温控达标率仪表
- [x] 双模式 TCO 曲线（标注盈亏平衡里程）+「自营 vs 货拉拉」对照表（引用 ADR-0009 成本参数）
- [x] 全图表中文标题/坐标轴/单位、悬停明细、统一配色、读数缓存
- [x] 全局筛选生效，整页无报错

## Comments

> *This was generated by AI during implementation（2026-09-14）.*

### 实现记录

**产出**：`pages/2_运输分析.py`（37 KB，680 行）。**未新增依赖**——`requirements.txt` 里没有
`streamlit-folium`，故用 `folium.Map` 生成后经 `components.v1.html` 渲染。

**6 个区块**：Folium 路线地图（方案一键切换，弹窗显示各点 HH:MM 时间窗与到达时刻并判
「窗内/早到/超窗」）、前后对比小倍数图（每图标注「↓=下降=改善」方向）、满载率分布（两套方案
各一张小倍数图、统一组距并锁 0–100% 便于并排比较——重叠直方图会互相遮挡，已写明理由）、
异常饼图 + 周度折线（把 `friday_late_rate` / `r07_anomaly_rate` 两列画在同一条轴上，旁边用
`anomaly_points.json` 显示池化 z 与逐周复现）、温控仪表（达标值从异常表求，不硬编码）、
TCO 曲线（盈亏平衡里程画竖线标注）+ 自营 vs 货拉拉对照表。

**颜色按实体固定分配**：优化前恒槽 0、优化后恒槽 1、柴油恒槽 2、纯电恒槽 3，周度埋点槽 4–7。
理由：读者一旦学会「优化前是蓝色」，一次筛选就换色会直接误导。

**外购地图底图**由 CartoDB 改为 OpenStreetMap——前者现已需要 API key，用它会引入一个
未登记的凭证依赖。

**验证**：`AppTest` 在真实运行时跑通（切「优化后」「柴油自购」「最近 90 天」、选 R07 片区）
→ **0 异常**；浏览器实测 33 张图、15 张表、1 个 Folium iframe。

### 收口复核（2026-09-14，17 号票期间）

页面最初**自行读盘**取了 `data/geo/poi.csv`（取数层当时没有该入口），绕开了「读数只走一层」
的约定——那样产物更新后这一处不会自动失效。已在取数层补 `poi_table()` 并把页面改调它。

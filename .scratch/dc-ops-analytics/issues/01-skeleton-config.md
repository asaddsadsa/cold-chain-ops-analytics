# 01: 工程骨架 + 共享 config（prefactor）

**What to build:** 任何人 clone 仓库后，装好依赖就能拿到一个结构完整、口径统一的工程底座：3.12 规定的目录树全部就位；`requirements.txt` 锁定全版本（Python 3.14.6 / pandas 2.3.3 / ortools 9.15 等已验证清单）并在干净 venv 安装成功；一个集中的 config 模块声明所有跨模块共享的「魔法数」——各数据层具名种子（SEED_WAREHOUSE/SEED_DELIVERY/SEED_SIM…）、相对路径、车辆与成本参数锚点、ABC 阈值（70/20/10）、敏感性低/中/高三档；Olist 九表从 archive 拷入 raw 目录；.env.example 模板、.gitignore 排除 .env 与大文件、README 骨架。

**Blocked by:** None (can start immediately)

**Status:** done（已实现并验收，见文末实现记录）

- [x] 目录结构与需求 3.12 一致（data/{raw,warehouse,geo,delivery,processed}、src、pages、report、tests）
- [x] requirements.txt 固定全部依赖版本；在 .venv（Python 3.14.6）`pip install -r` 全绿
- [x] pandas 锁 2.3.3（非 3.0），与 ADR-0006 一致
- [x] 集中 config 模块：具名种子（每数据层独立）、路径常量、车辆/成本参数锚点、ABC 阈值、敏感性三档全部在此声明，供下游票读取
- [x] Olist 九表从 archive 预置入 data/raw/olist/；保留「本地→kagglehub→报错」三段式获取入口
- [x] .env.example（AMAP_KEY=）就位；.gitignore 排除 .env、raw 大文件
- [x] 全局扫描确认无硬编码密钥、无本机绝对路径（一律相对路径）
- [x] README 骨架含五步复现框架占位

## Comments

> *This was generated by AI during triage.*

### Agent Brief

**Category:** enhancement
**Summary:** 搭建工程骨架与集中式 config 模块，作为全部下游数据层/模块票的共享底座（prefactor）。

**Current behavior:**
仓库仅有设计文档（CONTEXT.md、docs/adr/、data_sources_ledger.md、spec.md、本票目录）与 archive/ 下的 Olist 九表原始 CSV。无 src/、无 config、无依赖锁定文件、无目录骨架。

**Desired behavior:**
clone 后装好依赖即得到结构完整、口径统一的工程底座。集中 config 模块是唯一「魔法数」来源：各数据层具名种子、相对路径常量、车辆与成本参数锚点、ABC 阈值、敏感性低/中/高三档全部在此声明，下游票一律 import 读取而非各自硬编码。这样后续调整成本参数/阈值/种子只改一处。

**Key interfaces:**
- 集中 config 模块（建议 `src/config.py`）：导出具名种子常量（如 `SEED_WAREHOUSE`/`SEED_DELIVERY`/`SEED_SIM`）、路径常量（基于项目根的相对路径，禁止本机绝对路径）、车辆/成本参数锚点结构、`ABC_THRESHOLDS=(0.70,0.20,0.10)`、敏感性三档（低/中/高）参数集。
- requirements.txt：固定全部依赖版本，pandas 必须锁 2.3.3（见 ADR-0006），Python 3.14.6。
- .env.example：`AMAP_KEY=` 模板；.gitignore 排除 .env 与 raw 大文件。
- Olist 获取入口：保留「本地检测→kagglehub→报错指引」三段式契约（实现细节在 03 号票，本票只需把九表预置进 raw 目录并留好入口约定）。

**Acceptance criteria:**
- [ ] 目录树与需求 3.12 一致（data/{raw,warehouse,geo,delivery,processed}、src、pages、report、tests）
- [ ] requirements.txt 在干净 .venv（Python 3.14.6）`pip install -r` 全绿，pandas==2.3.3
- [ ] config 模块可被 import，导出种子/路径/成本锚点/ABC 阈值/敏感性三档，无散落在别处的硬编码
- [ ] Olist 九表从 archive 预置入 data/raw/olist/
- [ ] .env.example 与 .gitignore 就位；全局扫描无硬编码密钥、无本机绝对路径
- [ ] README 骨架含五步复现框架占位

**Out of scope:**
- 任何数据生成/清洗/分析逻辑（属 02–11 号票）
- Olist 三段式获取的具体实现（本票只预置数据 + 留入口约定，实现在 03）
- 看板、报告、测试用例（仅建 tests/ 空目录与骨架）

> *This was generated by AI during implementation.*

### 实现记录（2026-09-13）

**产出**：目录树（data/{raw,warehouse,geo,delivery,processed}、src、pages、report、tests）；`src/config.py`（集中种子/路径/成本锚点/ABC 阈值/敏感性三档 + 4 个 TCO 派生纯函数）；`src/__init__.py`；`requirements.txt`（锁全版本，pandas==2.3.3）；`.env.example`；`.gitignore`；`README.md` 骨架；`tests/conftest.py` + `tests/test_config.py`（21 测试全绿）；Olist 九表预置入 `data/raw/olist/`。

**验证**：`pip install -r requirements.txt` 全绿（PIP_EXIT=0）；全栈导入成功（pandas 2.3.3 / numpy 2.2.6 / ortools 9.15 / streamlit 1.63 等）；`pytest tests/` 21 passed；`py_compile` 通过；从 `C:\` 跨目录 import 成功、PROJECT_ROOT 正确解析（路径相对化生效）；密钥/绝对路径扫描干净。

**两处偏离 brief 的处置（均已确认）**：
1. **移除 streamlit-echarts**（用户确认）：实装时发现 `streamlit-echarts 0.7.0` 与 `streamlit 1.63` 运行时不兼容（停更孤儿包，import 即抛 StreamlitAPIException）。看板图表改用 Plotly 替代。新增 **ADR-0011**，并同步 spec 技术栈行、ADR-0006 包列表、requirements。属需求第 25 行授权的「减停更库、等价替代」，不删任何图表/KPI。
2. **成本派生函数改关键字参数**（自查发现）：原 `level` 单参数会同时驱动三个敏感性参数，无法表达台账要求的「工资/油价电价/租金**各**做低中高三档」的 one-at-a-time 分析。重构为 `driver_wage`/`energy_price`/`rent` 独立关键字参数（默认 mid），并修正 `breakeven_km` docstring 一处方向写反（低里程应柴油省）。新增 `test_one_at_a_time_sensitivity`、`test_breakeven_independent_of_driver_wage` 锁定行为。

**遗留 🔶（移交后续票）**：config 中成本参数为文档锚点值，台账第 4 节 URL 仍 🔶 待补（ADR-0009，实现期可联网环境补）；Olist 许可 URL 待 03 号票核实。

**下一步 frontier**：01 完成后解锁 **02（仓内仿真数据）、03（Olist 清洗）、04（高德地理）、05（Solomon 门禁）** 四票并行。

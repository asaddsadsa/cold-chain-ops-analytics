# 03: 数据层 B：Olist 真实订单获取与清洗

**What to build:** 运行一个脚本即拿到清洗后的 Olist 真实订单数据与两项供下游标定的统计产物。脚本实现三段式获取（优先检测本地 raw 已存在九表→否则 kagglehub 自动下载→两者都失败则报错并给手动下载与放置说明，严禁编造数据替代）；按规则清洗（全部时间字段转 datetime、剔除非 delivered、剔除时间逻辑矛盾记录、geolocation 同邮编多坐标按众数/均值聚合、按 order_id/product_id 关联），输出清洗结果到 processed 并给出清洗前后行数对比与剔除原因统计。同时从 products 表拟合商品重量/体积经验分布并保存（供配送情景抽样），统计 Olist 真实延迟率（实际送达晚于预计送达占比）写入台账（供在途异常概率标定）。

**Blocked by:** 01（工程骨架 + 共享 config，含 raw 九表预置）

**Status:** done（已实现并验收，见文末实现记录）

- [x] 三段式获取逻辑就位；本地已存在分支可跑通（archive 九表已预置）；下载失败时报错并给手动放置说明，不编造数据
- [x] 清洗规则全部实现：时间转 datetime、剔除非 delivered、剔除时间矛盾、邮编多坐标聚合、关联输出 clean_orders/order_items_clean
- [x] 输出清洗前后行数对比 + 剔除原因统计，损耗可解释
- [x] 商品重量/体积经验分布拟合并保存（供数据层 D 抽样）
- [x] Olist 真实延迟率统计并写入台账（供在途异常概率标定）
- [x] 台账登记 Olist 来源与许可类型（许可 URL 实现期到 Kaggle 核实补登）
- [x] 单测：清洗函数对手工构造小样例断言剔除逻辑正确

> *This was generated by AI during implementation.*

### 实现记录（2026-09-13）

**产出**：`src/olist_clean.py`（`load_olist_raw` 三段式获取 / `clean_orders`、`aggregate_geolocation`、`compute_delay_rate`、`fit_weight_volume_distribution` 等纯函数可独立调用）+ `tests/test_olist_clean.py`（15 测试）。产物落 `data/processed/olist/`：clean_orders.csv（96282 行）、order_items_clean.csv（109953 行）、clean_stats.json、olist_delay_rate.json、weight_volume_distribution.json、clean_report.md。

**关键实测数字**：
- 清洗：orders 99441→96282（剔 2963 非 delivered + 8 缺关键时间戳 + 188 时间矛盾）；order_items 112650→109953
- **Olist 真实延迟率 8.13%**（准时率 91.87%，n=96282）→ 已回填台账「在途异常概率标定」行（🔶→实测值）
- 重量对数正态：μ=6.689、σ=1.328、中位 700g、P95 9750g；体积：μ=8.846、σ=1.280、中位 6460cm³
- geolocation 100 万行 → 19015 邮编聚合；clean_orders 坐标关联率 99.7%；履约中位 10.23 天

**实现澄清（登记）**：需求「众数/均值聚合」坐标——连续量众数不适用，实现取**中位数**（抗离群），已在 clean_stats.json 与代码 docstring 注明。

**三段式获取**：本地九表齐全→直接读（当前路径）；缺失→kagglehub 下载（递归找文件拷入 raw）；双失败→RuntimeError 文案含 Kaggle URL、九个文件名、放置路径。无任何编造数据路径。

**测试缝**：spec 测试缝①（纯函数缝）——手工构造小样例断言每条剔除规则（非 delivered、四类时间矛盾、缺时间戳、一行只计第一原因）、坐标中位聚合、延迟率、对数正态拟合（μ 恢复真值 ±0.15）、非正值过滤、join 过滤。

**验证**：`pytest tests/` 53 passed（21 config + 17 数据层A + 15 数据层B）；`py_compile` 通过；`python -m src.olist_clean` 端到端跑通。

**遗留 🔶**：Olist 许可类型 URL（Kaggle 页面本机网络不可达，台账第 1 节已标注待补）；kagglehub 下载分支只验证了代码路径未实测（本地九表齐全走不到该分支，且本机 Kaggle 不可达）。

## Comments

> *This was generated by AI during triage.*

### Agent Brief

**Category:** enhancement
**Summary:** 实现 Olist 九表三段式获取 + 清洗 + 两项下游标定产物（重量体积经验分布、真实延迟率）。

**Current behavior:**
`data/raw/olist/` 已有预置九表（01 号票从 archive 拷入）。无清洗脚本、无 processed 产物。已勘察事实：orders 99441 行（delivered 96478，其余 shipped/canceled/unavailable/invoiced/processing/created/approved 共 2963 行待剔）；delivered_customer_date 有 2965 个空值；geolocation 同邮编多坐标确认存在；products 重量/体积四列非空 32949；reviews 评分 1–5。

**Desired behavior:**
一个脚本跑完产出：①清洗结果 `clean_orders`（仅 delivered、时间逻辑无矛盾、五时间戳齐全转 datetime）、`order_items_clean`（关联 product 重量体积与 customer 州）落 processed；②清洗前后行数对比 + 分原因剔除统计（非 delivered / 时间矛盾 / 关键字段缺失，各计多少行）落盘；③商品重量(g)/体积(cm³=长×宽×高)经验分布拟合参数保存（对数正态 fit 的 mu/sigma + 分位数表，供数据层 D 抽样）；④Olist 真实延迟率（实际送达 > 预计送达占比）统计写入 processed 并回填台账对应行。三段式获取：本地已存在（九表齐全）→直接用；缺失→kagglehub 下载；失败→报错给手动下载与放置路径说明。严禁编造数据替代。

**Key interfaces:**
- 清洗函数接受 DataFrame 返回 (清洗后 DataFrame, 剔除原因统计 dict)，可独立调用、可单测；
- 时间矛盾定义：实际送达早于下单、发货早于下单、审单早于下单、送达早于发货（任一条即剔）；
- geolocation 邮编聚合：同邮编多坐标取**中位数**（坐标是连续量，中位数比均值抗离群；众数对连续值不适用——这是对需求「众数/均值」措辞的实现澄清，登记台账）；
- 重量体积分布：对非空且 >0 的 product_weight_g 与体积拟合对数正态（scipy.stats.lognorm.fit），保存 mu/sigma/分位数(P5/P25/P50/P75/P95)为 JSON；
- 延迟率口径与 CONTEXT.md「真实准时交付率」互补：延迟率 = 1 − 真实准时交付率（实际送达 ≤ 预计送达为准时）。

**Acceptance criteria:**
- [ ] 三段式获取：本地分支跑通；模拟缺失时走 kagglehub 入口；双失败报错文案含手动下载 URL 与放置路径
- [ ] clean_orders 仅含 delivered、五时间戳为 datetime、无时间矛盾；order_items_clean 关联重量/体积/州成功
- [ ] 清洗前后行数对比 + 分原因剔除统计落盘 processed（JSON + Markdown）
- [ ] 重量/体积对数正态拟合参数 JSON 保存，分位数表合理（与 products 描述统计一致量级）
- [ ] 真实延迟率统计落盘并回填 data_sources_ledger.md「在途异常概率标定」行的 🔶 数值
- [ ] 单测：手工构造小样例断言每条剔除规则（非 delivered、四类时间矛盾、缺失时间戳）与延迟率计算正确

**Out of scope:**
- 真实履约 KPI 的多维下钻分析（09 号票）
- 配送情景的抽样实现（06 号票；本票只保存分布参数）
- reviews 交叉分析（09 号票）
